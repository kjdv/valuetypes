#include "templates.h"
#include <string>

namespace valuetypes {
namespace templates {

namespace {

const char templ[] = R"raw(${content})raw";

} // anonymous namespace

std::string_view ${name}() noexcept {
    return templ;
}
 
} // namespace templates
} // namespace valuetypes